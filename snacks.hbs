
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="../public/movies.css">
    <title>Movie Recommendations</title>
</head>
<body>
    <header>
        <nav>
            <h1>AMC</h1>
        </nav>
    </header>   
    <main>
        <div class="searchbar">
            <h1>Search and get recommendations</h1>
            <p>Search through the database and get recommendations for movies</p>
            <div class="box">
                <form action="/api/search" method="get" id="searchForm">
                    <input type="text" id="search" name="movieName" placeholder="Select a movie, genre or actor" required>
                    <button type="submit" id="button">SEARCH</button>
                </form>
            </div>
        </div>

        <div class="Showing">
            {{#if movies.length}}
                {{#each movies}}
                    <div class="movie">
                        <img src="https://image.tmdb.org/t/p/w185/{{poster_path}}" alt="{{original_title}} Poster">
                        <h1>{{original_title}}</h1>
                    </div>
                {{/each}}
            {{else}}
                <p>No movies found.</p>
            {{/if}}
        </div>
    </main>
    <script src="../front.js"></script>
    <script type="text/hbs" src="index.hbs"></script>
</body>
</html>
